#!/bin/bash

if [ -n "$1" ] ;
  then echo "clocking into $1..."
  else echo "no environment!"; exit 1
fi;

mkdir -p $HOME/tmp/env
touch $HOME/tmp/env/$1

NO_VERSION=1 NO_SHELL=1 source set-$1-env
bash

